import {UserId} from './UserId';

export class ThemeData {
  constructor(
    subscribers: string[],
    posts: string[],
    _id: string,
    themeName: string,
    userId: UserId,
    created_at: string,
    updatedAt: string) {
  }
}

