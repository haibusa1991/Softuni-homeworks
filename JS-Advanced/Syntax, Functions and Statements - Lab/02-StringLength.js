function solution(in1, in2, in3) {
    let totalLen = in1.length + in2.length + in3.length;
    console.log(totalLen);
    console.log(Math.floor(totalLen/3));
}

solution('chocolate', 'ice cream', 'cake');
solution('pasta', '5', '22.3');
